name: Publish to GHCR

env:
  RELEASE: edge

on:
  push:
    branches:
      - 3-24.04
  workflow_dispatch:

jobs:
  ci-tests:
    uses: ./.github/workflows/ci.yaml
  publish:
    needs: ci-tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Docker
        run: |
          sudo snap install docker
          sudo addgroup --system docker; sudo adduser $USER docker
          newgrp docker
          sudo snap disable docker; sudo snap enable docker
      - name: Install rockcraft
        run: |
          sudo snap install rockcraft --classic
      - name: Install yq
        run: |
          sudo snap install yq
      - name: Download rock file
        uses: actions/download-artifact@v4
        with:
          name: opensearch_dashboards_rock_amd64
          path: .
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ secrets.GHCR_USER }}
          password: ${{ secrets.GHCR_TOKEN }}
      - name: Import and push to GHCR
        run: |
          version="$(yq .version rockcraft.yaml)"
          base=$(yq .base rockcraft.yaml)
          base="${base#*@}"
          tag=${version}-${base}-${{ env.RELEASE }}

          echo "Publishing opensearch-dashboards:${tag}"
          sudo rockcraft.skopeo --insecure-policy copy \
            oci-archive:opensearch-dashboards_${version}_amd64.rock \
            docker-daemon:ghcr.io/canonical/opensearch-dashboards:${tag}
          docker push ghcr.io/canonical/opensearch-dashboards:${tag}
